/*write a program which will show the difference in working of = and == operator*/
#include<stdio.h>
#include<conio.h>
void main()
{
    int a,b;
    a=3,b=4; /* = is a assignment operator
                        so it is use to assign any value to a_variable */


    a==b ? printf("TRUE"):printf("FALSE");
    /* == relation operator it is use to compare any variable */
    getch();

}
