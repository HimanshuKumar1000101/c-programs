#include<stdio.h>
#include<conio.h>
void main()
{
    char str[20];
    int str1[5];
    printf("enter the name and course code\n");
    gets(str);
    gets(str1);
    printf("the name is %s\n",str);
    printf("the course code is %d",str1);
getch();
}
